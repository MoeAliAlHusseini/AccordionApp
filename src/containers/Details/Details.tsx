import React from 'react';

import { View, Text } from 'react-native';

const Details = () => {
    return (
        <View>
            <Text>Title</Text>
        </View>
    );
};

export default Details;
